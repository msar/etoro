# Optional: port only. Prefer the in-app login screen to store credentials
# in server/data/credentials.json (gitignored). Env vars still work as overrides.
PORT=4000

# Optional env overrides (leave blank if using the login screen):
# ETORO_API_KEY=
# ETORO_USER_KEY=
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
